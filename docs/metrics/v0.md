# NPU 指标盘点清单 v0（版本化）· docs/metrics/v0.md

> **版本：v0**　**日期：2026-08-16（UTC）**
> **状态声明：尚未上真机——表中全部"期望值或口径"为报告引用/参考口径，非实测数字；真机回填状态一律「待回填」。**
>
> 依据：[04 · 风险清单与路线图 v0 §3 NPU 全功能验收矩阵](./../research/04-roadmap-and-risk.md)、
> [02 · 固件生态与支持状态](./../research/02-firmware-ecosystem.md)、[01 · 硬件考据](./../research/01-hardware.md)、[CONTEXT.md](../../CONTEXT.md)
> 采集端：`tools/metrics/collect.sh` v0（dry-run 已通过：8 模块 53 指标位，全部 PENDING_DEVICE 占位，退出码 0）
> 回填流程与版本化规则：`docs/metrics/backfill-template.md`

## 0. 状态速览

| 项 | 值 |
|---|---|
| 清单版本 / 日期 | **v0 / 2026-08-16** |
| 采集脚本 | tools/metrics/collect.sh v0（模块：npu / mt76 / flow / eth / cpu / iperf3 / alerts / thermal） |
| 真机回填 | 未开始（M0 购机后按 backfill-template.md 回填；当前全部「待回填」） |
| 指标规模 | 脚本采集 53 指标位（npu 8 / mt76 16 / flow 6 / eth 5 / cpu 4 / iperf3 5 / alerts 6 / thermal 3）+ 操作模板 4（见 §2/§4/§5）+ 构建态核对 5（见 §1–§3） |
| 版本化 | v0→v1 触发规则一句话：**指标集/口径有任何新增、删除或修改 → bump v1（AI 改清单 + 人类真机数据合入，带 changelog）**；仅回填数值变化不 bump |
| 覆盖范围 | 04 §3 四子验收 ①②③④ + ⑤AP/桥接专项 + 核心口径 5 条 + 版本化闭环（映射见 §8） |

**回填状态取值**（本 v0 全文一律以「待回填」合并标记；回填后按 backfill-template.md §2 词表改写）：
现状粒度词表（backfill-template.md §2，五值）：`未采集·脚本已含 / 未采集·模板待手动 / 未采集·操作模板 / 未采集·构建态 / 未采集·面板兜底`——v0 未上真机，统一合并记「待回填」；回填时按实际采集来源拆分改写为 `已采集·通过 / 已采集·未通过 / 不适用(NA)`（NA 须在 backfill §3 备注原因：命令缺失 / 接口不存在 / 场景未跑）。**两处词表一致，粒度以 backfill-template.md §2 为准。**

---

## 1. 子验收 ① WED/NPU 路径（含 6GHz）

> 词条①（04 §3）：WED/NPU 路径含 6GHz；核心口径②（6GHz 回程 offload 开启 tx failed=0）的路径侧；
> R4 缓解（CVE-2025-68360 面）与 M2 验收「CVE-2025-68360/22061 回归」的运行态证据。

| 指标名 | 采集命令（脚本条目） | 期望值或口径 | 真机回填状态 | 来源 |
|---|---|---|---|---|
| `npu.fw_version` | npu 模块：`dmesg \| grep -iE "npu.*fw version\|fw version"` | dmesg/启动日志含 NPU 固件版本号（出厂参考 `0.1111`，与实机对照） | 待回填 | 01 §1.25；04 §3 指标来源 |
| `npu.firmware_glue` | npu 模块：`ls /lib/firmware/mediatek/ \| grep en7581` | `en7581_MT7996_npu_rv32.bin` + `en7581_MT7996_npu_data.bin` 在场（MT7996 Wi-Fi 前端绑定；只见到 `en7581_npu_rv32.bin`=MT7992 误配 → 告警） | 待回填 | 01 §1.25；02 §2 打包 PR #22289 |
| `npu.reserved_mem` | npu 模块：`grep -i npu /proc/iomem` | 保留区 `npu-binary`(10MiB) 与 `npu-pkt`(45MiB) 均在 | 待回填 | 01 §1.25 |
| `npu.dts_node` | npu 模块：`ls /proc/device-tree/ \| grep npu` | `airoha,en7581-npu` @0x1E900000 节点在场 | 待回填 | 01 §1.25 |
| `npu.dmesg_ring` | npu 模块：`dmesg \| grep -i npu \| tail -20` | NPU 启动/运行日志无崩溃痕迹（wed 崩溃、0x 异常）——CVE-2025-68360 运行态侧证据（**口径注：脚本 kw=`npu` 仅要求输出含 npu 关键词、非 ABSENT——「无崩溃」为人工降级判定，回填时需人工读日志确认；v1 可改 kw=ABSENT + 崩溃关键字双检**） | 待回填 | 02 §2；04 §3 CVE 在场 |
| `mt76.<phy>.is_6ghz` | mt76 模块：`iw phy info` 频率列表 max≥5955MHz 推导 | 6GHz radio 被正确标记（6GHz offload 面，CVE-2025-68360 相关） | 待回填 | 02 §2（6GHz/EHT320 可用）；04 §3 词条① |
| `alerts.dmesg_wed` | alerts 模块：`dmesg \| grep -iE "wed\|mt7996" \| grep -iE "crash\|bug\|fail\|error"` | 无 WED 相关崩溃/报错（6GHz offload 开启下；期望=空输出/ABSENT） | 待回填 | 02 §2 CVE-2025-68360；04 §3 R4 |
| CVE-2025-68360 修复在场 | 构建态核对：`git log \| grep -i CVE-2025-68360`（脚本不含） | 分叉 mt76 含修复提交（对照 NVD/上游补丁） | 待回填 | NVD CVE-2025-68360（2025-12-24）；02 §2；04 §3 R4/核心口径⑤ |
| mt76 NPU 层提交在场 | 构建态核对：分叉 mt76 版本对照（脚本不含） | ≥ NPU 通用层（2025-10-17）/ MT7996 NPU enable（2026-01-22）/ WED+NPU 队列修复（2026-07-23）/ NPU&PPE 仅 MMIO（2026-07-20） | 待回填 | 04 §3 指标来源；02 §2 |

## 2. 子验收 ② PPE 硬 NAT 满载

> 词条②（04 §3）；核心口径①（双 10G iperf3 双向近线速）与核心口径③（NPU 转发 ≥10Gbps）；
> FlowSense（PPPoE/VLAN/AP 模式/XFRM）与 airoha target 提交史（HW-GRO/LRO/PPE MTU）在此验收。

| 指标名 | 采集命令（脚本条目） | 期望值或口径 | 真机回填状态 | 来源 |
|---|---|---|---|---|
| `flow.bin` | flow 模块：`command -v flowsense flowm flowmd` | FlowSense 用户态命令在场（PPE 硬 NAT 前哨） | 待回填 | 02 §4；04 §3 词条② |
| `flow.uci` | flow 模块：`uci show flowsense; uci show flowm` | 配置完整且 PPPoE/VLAN/AP 模式/XFRM 通道可见 | 待回填 | 02 §2 社区 FlowSense；04 §3 指标来源 |
| `flow.flows` | flow 模块：`flowm dump \| head -40` | 硬加速流表非空、逐流可读（AP 模式下仍可见，交叉 ⑤） | 待回填 | 04 §3 词条②⑤ |
| `flow.flows_pattern` | flow 模块：`flowm dump \| grep -iE "pppoe\|vlan"` | PPPoE/VLAN 特征流可辨识（**PPPoE 硬卸载无直接实测文档→仅观测，进 v1 验收需真机流表**） | 待回填 | 02 §2；04 §3 指标来源 |
| `flow.log` | flow 模块：`logread \| grep -iE "flowsense\|flowm\|ppe"` | 初始化成功、无 PPE 报错；无 CVE-2025-22061 对应 kernel warning（**口径扩展建议：CVE-2025-22061 为 HTB offload 叶子删除 qid warning，现 grep 关键字组 `flowsense\|flowm\|ppe` 可能漏网——建议补 `htb\|qid\|leaf\|offload`；collect.sh v0 未含 → 已记 §7 改进项，v1 改脚本关键字**） | 待回填 | 02 §7；04 §3 CVE 在场 |
| `flow.ppe_debugfs` | flow 模块：`ls /sys/kernel/debug/{flow,airoha/ppe}` | PPE 计数节点在场（交叉 ④ 可观测） | 待回填 | 04 §3 词条②④ |
| `eth.port_list` | eth 模块：`ls /sys/class/net \| grep -E "^eth\|^lan\|^wan"` | 网口集齐：2×10G（RTL8261BE）+ 2×1G（MT7530） | 待回填 | 02 §0 网口；02 §5 PHY |
| `eth.port_speed` | eth 模块：sysfs `…/speed` 遍历 | 双万兆口 Speed=10000（RTL8261BE）、双千兆=1000（MT7530） | 待回填 | 02 §0；04 §3 核心口径① 载体 |
| `eth.port_link` | eth 模块：sysfs `…/carrier` 遍历 | 验收时双 10G 链路 carrier=1（Link up） | 待回填 | 04 §3 核心口径① |
| `eth.ethtool_dump` | eth 模块：`ethtool eth0/eth1 \| grep -iE "Speed\|Link detected"` | ethtool 可见双 10G 口 Speed: 10000Mb/s 且 Link detected: yes（依赖 ethtool 已装） | 待回填 | 02 §0；04 §3 核心口径① |
| `eth.phy_driver` | eth 模块：`ethtool -i eth0/eth1 \| grep -i driver` | 10G PHY 驱动 `rtl8261` 在册 | 待回填 | 02 §0/§5 |
| `iperf3.bin_present` | iperf3 模块：`command -v iperf3` | iperf3 二进制在场（路由器与对端双侧）——核心口径①/③/④ 吞吐位点的工具链前置检查；缺失则该位 NA 且各 `iperf3.template.*` 无法手动执行 | 待回填 | 04 §3 核心口径① 工具链 |
| `iperf3.template.dual10g` | iperf3 模块预留模板（**不自动执行**）：`iperf3 -c <PEER_10G_IP> -t 60 -P 4 --bidir -O 5 --json` | **核心口径①：双 10G iperf3 双向近线速**（报告原文仅「近线速」；≥9.4Gbps/向 为补充量化的**参考值**、非 04 §3 硬性阈值，以实机为准） | 待回填 | 04 §3 核心口径①；02 §0 |
| `iperf3.template.udp_nat` | iperf3 模块预留模板：`iperf3 -u -b 0 -t 60 --json` | **核心口径③：NPU 转发 ≥10Gbps**；UDP 硬 NAT 满载 + PPE 流表命中 | 待回填 | 04 §3 核心口径③、词条② |
| 卸载命中计数 | 操作模板：吞吐期间 `flowm`/PPE debugfs 计数快照（见 backfill-template §1 第 4 步） | M2 验收「卸载命中计数」：命中计数随流量增长、与吞吐对应 | 待回填 | 04 §3 M2 验收；词条② |
| airoha target 特性在场 | 构建态核对：分叉内核 kconfig/git（脚本不含） | ≤ set：HW-GRO（2026-06-16）/ LRO（2026-05-25）/ PPE MTU L2 开销（2026-06-19） | 待回填 | 04 §3 指标来源 |
| CVE-2025-22061 修复在场 | 构建态核对：`git log \| grep -i 22061`（脚本不含）+ 运行态 `flow.log` 无对应 warning | 分叉 net: airoha 含修复提交且运行无 HTB offload 叶子删除 qid warning | 待回填 | 02 §7；NVD CVE-2025-22061 |

## 3. 子验收 ③ 与 mt76/社区特性共存无降级

> 词条③（04 §3）；核心口径④（10G↔Wi-Fi 场景 CPU<5%，论坛实测圈 0–1%）；
> 「6GHz 回程稳定化」运行态告警清零（恩山手法⑤ 项）亦在此核对。

| 指标名 | 采集命令（脚本条目） | 期望值或口径 | 真机回填状态 | 来源 |
|---|---|---|---|---|
| `cpu.top_snapshot` | cpu 模块：`top -b -n1 \| head -20` | **核心口径④：10G↔Wi-Fi 并发 iperf3 时 CPU <5%**（论坛实测 0–1%）；须与 `wifi10g` 模板同刻截取（**口径注：留档型、非判定——脚本 kw=`-`，OK 仅表示采集成功 ≠ 达标；CPU<5% 需人工读快照判**） | 待回填 | 04 §3 核心口径④；01 §1.28 |
| `cpu.loadavg` | cpu 模块：`cat /proc/loadavg` | 空载 <1.0；吞吐测试期间另留同刻对照（**口径注：留档型、非判定——脚本 kw=`-`，OK ≠ 达标；<1.0 为人工对照口径**） | 待回填 | 04 §3 核心口径④ 佐证 |
| `cpu.pidstat` | cpu 模块：`pidstat 1 1`（procps-ng 可选） | NPU/WED 线程占用分布可辨（未装→NA 不阻塞） | 待回填 | 04 §3 词条③ |
| `cpu.softirqs` | cpu 模块：`head /proc/softirqs` | offload 开时 NET_RX 软中断增长平缓（开关对照佐证，交叉 ④） | 待回填 | 04 §3 词条④ |
| `iperf3.template.wifi10g` | iperf3 模块预留模板：`iperf3 -s`（路由器 10G LAN 口）+ STA 经 Wi-Fi `iperf3 -c` | **核心口径④：10G↔Wi-Fi 1.1–1.4Gbps 且 CPU<5%**（论坛实测 2026-03-22）（**词条③直接证据要求：一键开关 UI 关态下同点位再采集一次，与开态构成 on/off 对照——开态吞吐不劣于关态即「与 mt76/社区特性共存无降级」直接证据；回应「仅间接证据」问题；对照采集流程见 backfill-template §1 第 3 步**） | 待回填 | 04 §3 核心口径④；01 §1.28 |
| `alerts.dmesg_oom` | alerts 模块：`dmesg \| grep -iE "out of memory\|oom-kill\|killed process"` | 无 OOM 痕迹（期望=空输出/ABSENT） | 待回填 | 02 §7 稳定性；04 §3 M3 稳定运行 |
| `alerts.dmesg_panic` | alerts 模块：`dmesg \| grep -iE "kernel panic\|Oops\|BUG:\|unable to handle"` | 无内核崩溃/Oops（期望=空输出/ABSENT）（**口径扩展建议：M3「无 UBI/I/O 错误」词位应与本扫描相邻核对——建议补 grep 关键字 `ubi\|I/O error\|io error`（或单列 `alerts.dmesg_ubi`）；collect.sh v0 未含 → 已记 §7 改进项，v1 补采集位/关键字**） | 待回填 | 02 §2（驱动快速收敛期）；04 §3 |
| `alerts.dmesg_airtime` | alerts 模块：`dmesg \| grep -i airtime_link_metric` | 无 `airtime_link_metric_get` 报错（恩山稳定化验收项，期望=空输出/ABSENT） | 待回填 | 02 §7 ⑤；04 §3 M3 |
| mt7996 全系 NVD CVE 修复在场 | 构建态核对：git 层逐条 grep（脚本不含，逐条打勾） | **核心口径⑤：全部 mt7996 相关 NVD CVE 修复在场**（NVD 24 条命中：2024-35909/38563/47681/47714、2025-38156/38281/38316/38343/38599、2023-53203 等） | 待回填 | 04 §3 核心口径⑤；02 §7 |

## 4. 子验收 ④ 可观测可开关

> 词条④（04 §3：可观测、可一键开关）；04 §3 闭环机制「AI 出全量抓取脚本」的面板补充入口。

| 指标名 | 采集命令（脚本条目） | 期望值或口径 | 真机回填状态 | 来源 |
|---|---|---|---|---|
| `npu.debugfs_tree` | npu 模块：`ls -R /sys/kernel/debug/{airoha,flow,ppe}` | debugfs airoha/flow/ppe 可枚举，含统计/队列节点 | 待回填 | 04 §3 词条④ |
| `npu.luci_panel` | npu 模块：`opkg list-installed \| grep -iE "airoha-npu\|flowsense"` | `luci-app-airoha-npu` 已装（面板可读=可观测/开关入口） | 待回填 | 04 §3 词条④；02 §4（naoki66 等社区构建内置） |
| `npu.luci_counters` | npu 模块：`ubus list \| grep -i npu; uci show airoha-npu; grep -rE "npu\|offload\|hit\|cnt" /sys/kernel/debug/airoha` | luci-app-airoha-npu 面板/底层卸载计数可读（命中/开关状态；接口名以实机面板为准滚动收录） | 待回填 | 04 §3 闭环机制；核心口径③ 计数侧 |
| `flow.ppe_debugfs` | flow 模块（交叉 §2）：PPE 计数节点在场 | 同上 | 待回填 | 04 §3 词条②④ |
| `cpu.softirqs` | cpu 模块（交叉 §3）：offload 开关对照 NET_RX 增长 | 同上 | 待回填 | 04 §3 词条④ |
| 一键开关对照 | 操作模板：NPU offload **关→采集→开→采集** 双组对照（开关手段：uci flowsense 启停 / debugfs 写 / 面板开关，回填注明用哪个） | M2 验收「一键开关对照」：吞吐/CPU/命中率 开≠关 且 开侧 ≥ 关侧（卸载确实生效） | 待回填 | 04 §3 M2 验收、词条④ |

## 5. 子验收 ⑤ AP/桥接模式专项

> 词条⑤（04 §3）：FlowSense AP 模式流表、WED per-radio 计数、802.11s 回程卸载、
> 逐流/逐客户端命中；核心口径②（6GHz 回程 offload 开启 tx failed=0）与 M3 旗舰口径在此。

| 指标名 | 采集命令（脚本条目） | 期望值或口径 | 真机回填状态 | 来源 |
|---|---|---|---|---|
| `mt76.phy_count` | mt76 模块：`ls /sys/kernel/debug/ieee80211/phy* \| wc -l` | = 3（2.4/5/6GHz 各一 phy） | 待回填 | 02 §0 三频射频 |
| `mt76.<phy>.band` | mt76 模块：`iw phy info \| grep "^Band "` | Band 1/2/3 齐备 | 待回填 | 02 §0 |
| `mt76.<phy>.tx_failed` | mt76 模块：`grep -i "tx failed" mt76/{tx_stats,stats}` | **核心口径②：tx failed=0**（含 6GHz 回程 radio） | 待回填 | 04 §3 核心口径②；02 §7 恩山⑤ |
| `mt76.<phy>.wed_queue` | mt76 模块：`ls mt76/ \| grep -iE wed` | WED 队列/统计节点在场（per-radio 计数，含 6GHz radio） | 待回填 | 04 §3 词条⑤ |
| `mt76.<phy>.mt76_debugfs` | mt76 模块：`cat mt76/{queues,tx_stats}` | 队列/统计可读（原文留档） | 待回填 | 04 §3 词条①④ |
| `flow.flows`（AP 模式） | flow 模块（交叉 §2）：`flowm dump` | AP/桥接工作模式下硬加速流表**仍可见、非空** | 待回填 | 04 §3 词条⑤ |
| `iperf3.template.backhaul` | iperf3 模块预留模板：`iperf3 -c <MESH_NODE_IP>` + `ping -c 20 -i 0.2` | **核心口径②/M3：6GHz 802.11s 回程 offload 开启下吞吐/时延基准跑稳**（双节点统一 Mesh ID/SAE/监管域/ch37/EHT320） | 待回填 | 04 §3 核心口径②；02 §7 恩山手法⑤ |
| 回程 mesh ESTAB | 操作模板：`iw dev` 双节点 mesh 状态 | mesh 双节点 ESTAB（M2 验收「mesh 组网成功率」） | 待回填 | 04 §3 M2 验收；02 §7 |
| 逐流/逐客户端命中 | 操作模板：per-STA iperf3 + `flowm` 逐流 dump | 每条客户端流在流表/计数中可辨识 | 待回填 | 04 §3 词条⑤ |
| `alerts.max_inactivity` | alerts 模块：`uci show wireless \| grep -iE "max_inactivity\|disassoc_low_ack"` | `max_inactivity=86400` / `disassoc_low_ack=0` 在场（恩山 6GHz 回程稳定化手法④；M3 口径，非 04 §3 硬性） | 待回填 | 02 §7 ④ |
| `alerts.governor` | alerts 模块：`cat …/scaling_governor` | `performance`（恩山稳定化手法④；M3 口径） | 待回填 | 02 §7 ④ |

## 6. M3 环境口径（附加，非 04 §3 硬性）

> 归档为环境指标：NPU 满载热特性与风扇（02 §7「温度 52–57°C 常态」；04 §3 M3 验收）。

| 指标名 | 采集命令（脚本条目） | 期望值或口径 | 真机回填状态 | 来源 |
|---|---|---|---|---|
| `thermal.zones` | thermal 模块：`/sys/class/thermal/thermal_zone*` 遍历 | 温度可读（°C）；**常态 52–57°C** | 待回填 | 02 §7 ⑤；04 §3 M3 |
| `thermal.hwmon` | thermal 模块：`cat /sys/class/hwmon/hwmon*/temp*_input` | hwmon 温度通道可读 | 待回填 | 02 §7 温度口径 |
| `thermal.fancontrol_cfg` | thermal 模块：`uci show fancontrol; cat /etc/config/fancontrol` | `luci-app-w1700k-fancontrol` 配置在场 | 待回填 | 02 §4 社区构建（来源 repo） |

> **M3 验收词补充说明**（04 §3 M3 验收原文，v0 起登记；均非单点脚本采集位）：
> - **「无 UBI/I/O 错误」**：运行态证据 = alerts/dmesg 扫描须纳入 ubi/io 相关错误关键字（如 `grep -iE "ubi.*(error|corrupt)|I/O error|io error" dmesg`）。collect.sh v0 的 alerts 模块仅扫 oom/panic/wed/airtime，**未含该关键字组** → 已记 §7 改进项，v1 补采集位（或并入 `alerts.dmesg_panic` 关键字组）。
> - **「连续稳定运行 7×24h」**：由 `docs/metrics/runs/` 归档承载——每个 run 记录连续运行时长与日志/截图证据（状态位 `M3.run_duration`，非脚本指标）；归档命名/元信息约定见 `runs/README.md` 〔来源: 04 §3 M3 验收〕。

## 7. 来源不全 → 改进项（不进验收，v0 起即登记）

> 收录规则（backfill-template.md §5.2）：**没有直接实测来源的条目只进改进项，不进 v1 验收**；真机数据补上后才转正。

| 项 | 缺口 | 处置 |
|---|---|---|
| PPPoE 硬件卸载 | 无直接实测文档（01 §9「未能确认」）；FlowSense 声称支持 | 等真机 `flow.flows_pattern`/`flow.flows` 数据；有实测再入 v1 验收 |
| 6GHz 天线口径（4×4+1 vs 4×4:4） | 硬件证据两说（01 §9.4） | 不进验收指标；拆机/FCC 照片后续核对 |
| CPU 0–1% 论坛数字复现 | 需真机 iperf3 才能判 | 真机回填后进 v1 数据对照 |
| UBI/I/O 错误扫描 | collect.sh v0 alerts 模块不含 ubi/io 关键字 grep（M3「无 UBI/I/O 错误」词位，见 §6 补充说明）；真机 NAND/UBI 层错误会漏扫 | v1 在 alerts 模块补 `alerts.dmesg_ubi` 或并入 `dmesg_panic` 关键字组（`ubi\|I/O error\|io error`） |
| `flow.log` CVE-22061 关键字组 | collect.sh v0 `flow.log` grep `flowsense\|flowm\|ppe` 可能漏 CVE-2025-22061 对应 HTB/qid warning（见 §2 行口径扩展建议） | v1 补 `htb\|qid\|leaf\|offload` 关键字 |

## 8. 可追溯性核对（04 §3 逐条 ↔ 本清单）

| 04 §3 条款 | 覆盖行 | 状态 |
|---|---|---|
| 词条① WED/NPU 路径含 6GHz | §1 全部 + `mt76.<phy>.wed_queue`/`is_6ghz` + `iperf3.template.backhaul` | ✅ |
| 词条② PPE 硬 NAT 满载 | §2 全部 + `iperf3.template.dual10g/udp_nat` + 卸载命中计数 + airoha target 构建态 | ✅ |
| 词条③ 与 mt76/社区特性共存无降级 | §3 全部 + `npu.dmesg_ring` + `mt76.<phy>.tx_failed` + `iperf3.template.wifi10g` | ✅ |
| 词条④ 可观测可开关 | §4 全部（debugfs / luci 面板与计数 / 一键开关对照） | ✅ |
| 词条⑤ AP/桥接专项 | §5 全部（AP 模式流表 / WED per-radio / 802.11s 回程 / 逐流命中 / mesh ESTAB） | ✅ |
| 词条·指标版本化与实机盘点闭环 | 本文件版本化头 + backfill-template.md §4/§5 | ✅ |
| 核心口径① 双 10G iperf3 双向近线速 | `iperf3.template.dual10g` + `eth.*` 链路侧 | ✅ |
| 核心口径② 6GHz 回程 offload 开启 tx failed=0 | `mt76.<phy>.tx_failed` + `iperf3.template.backhaul` + 回程 mesh ESTAB | ✅ |
| 核心口径③ NPU 转发 ≥10Gbps | `iperf3.template.dual10g/udp_nat` + 卸载命中计数 + `npu.luci_counters` | ✅ |
| 核心口径④ 10G↔Wi-Fi CPU<5% | `cpu.top_snapshot` + `cpu.loadavg` + `iperf3.template.wifi10g` | ✅ |
| 核心口径⑤ 全部 mt7996 相关 NVD CVE 修复在场 | 构建态核对 3 行（CVE-2025-68360 / 2025-22061 / mt7996 全系）+ 运行态 alerts/npu/flow 日志 | ✅ |
| M2 验收清单（双 10G / mesh / 回程基准 / 卸载命中 / CVE 回归 / tx failed=0 / CPU<5% / 一键开关） | 见 §1–§5 对应行 | ✅ |
| M3 旗舰口径（mesh ESTAB / tx failed=0 / 回程基准 / 无 UBI-I/O 错误 / 7×24h 连续运行 / 温度 52–57°C） | §5 mesh 与回程行 + §6 温度/环境 + §6 补充说明（runs/ 归档承载 7×24h 与 UBI-I/O 证据） | ✅ |
| 闭环机制（实机面板为准滚动收录） | backfill-template.md §5 + 本 §7 | ✅ |

> 覆盖结论：**04 §3 全部条款（四子验收 + ⑤专项 + 核心口径 5 条 + M2 验收 + 闭环机制）均已映射到可采集指标或对应机制文档，无遗漏。**
> 本清单所有期望值均标注 `[来源]`，未上真机，无任何编造实测数字。