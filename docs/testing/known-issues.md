# Known Issues · XR1710G alpha（P2/P3 留档）

> 用途：alpha 期「已知 bug 清零」分级制的 P2/P3 收容所（P0/P1 必须修完，见
> `docs/plan/alpha-campaign.md` §0）。每项一经确认立即建档，不移除，只标记
> `RESOLVED`（附版本）。归属按「社区基线参照」：同缺陷在社区 6.18.41 线成立 = `upstream`，
> 不成立 = `ours`；无法判定 = `unverified`。

## 约定

- 标题格式：`[P2|P3] <一句话缺陷>（<version-observed>）`
- 字段：复现步骤 / 期望 / 实际 / 归属 / 影响面 / 状态 / 会话
- 每轮回归前人工核对 `status=OPEN` 项是否在新镜像复现

---

## Open

（无）

## Resolved

（无）