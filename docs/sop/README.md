# docs/sop · 救砖与真机操作 SOP 目录

> 状态总览：全部为 **草稿 / 待实机验证**（M0 交付物，人类最小集配套文档）。未经真机验证的地址、文件与命令一律以 `docs/sop/brick-recovery.md` §10 命令核对表为准（标 ⚠️ 待实机验证 的项变动手前必须真机确认）。

## 文件

- **brick-recovery.md**（草稿 v0.2，待实机验证）——救砖主 SOP：危险红区表（后果分级）、四档分级恢复流程（①系统可启动→Web 回退 / ②U-Boot 可中断→串口链 / ③仅剩 YYH 恢复页 / ④无引导→编程器）、YYH 镜像备份与回刷命令序列、一机一路线互刷边界实验记录表、双系统（原厂↔自建基线）切换检查表、命令核对表、未能确认清单与待实机验证清单（V-1…V-16，真机交接初稿）。救砖一切动作按此执行。
- **mutual-flash-experiment-template.md**（模板，待真机回填）——YYH ↔ upstream chainloader 互刷边界实验的专项记录模板（动作字典 A1/A2/B1/B2/B3/C1、恢复入口速查表、回退可达性矩阵），M0 真机填表后产出「互刷边界实验报告」。
- **scratch-pad-template.md**（模板，待真机回填）——真机现场通用记录模板（时间/操作者/现象/截图位），救砖与切换操作边做边记，与互刷实验模板配套使用。

## 使用顺序

1. 判档位（brick-recovery.md §0）→ 2. 过红区表（§1）与命令核对表（§10）→ 3. 做备份（§2）→ 4. 按档位执行（§4–§7）→ 5. 边做边填 scratch pad，实验填互刷模板。